var React = require('react');

var Timer = React.createClass({
    render: function () {
        return (
            <div>
                <h3>I am a Timer component</h3>
            </div>
        );
    }
});

module.exports = Timer;